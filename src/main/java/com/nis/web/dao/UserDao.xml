<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nis.web.dao.UserDao">
	
	<cache eviction="LRU" flushInterval="60000" size="512" readOnly="true">
	</cache>
	<!-- 通过别名映射对象 -->
	<select id="getUserById" parameterType="long" resultType="sysUser">
		select 
			id as "id",
			login_id as "loginId",
			email email,
			create_time createTime,
			status status
		from sys_user where id=#{id}
	</select>
	
	<select id="getUserByLoginName"  resultMap="userResultMap">
		select 
			id,
			login_id,
			name,
			email,
			create_time,
			status
		from sys_user where login_id=#{loginName}
	</select>
	
	<!-- map 转为对象 -->
	<resultMap type="sysUser" id="userResultMap">
		<id property="id" column="id"/>
		<result property="loginId" column="login_id"/>
		<result property="name" column="name"/>
		<result property="email" column="email"/>
		<result property="createTime" column="create_time"/>
		<result property="status" column="status"/>
	</resultMap>
	
	<resultMap type="sysUser" id="userWithRelationResultMap">
		<id property="id" column="user_id"/>
		<result property="loginId" column="login_id"/>
		<result property="name" column="user_name"/>
		<result property="password" column="password"/>
		<result property="email" column="email"/>
		<result property="createTime" column="user_create_time"/>
		<result property="status" column="user_status"/>
		<collection property="userDeptList" ofType="sysDepartment">
			<id property="id" column="depart_id"/>
			<result property="deptNo" column="dept_no"/>
			<result property="deptName" column="dept_name"/>
			<result property="deptNameEn" column="dept_name_en"/>
			<result property="deptNameShort" column="dept_name_short"/>
			<result property="parentId" column="parent_id"/>
			<result property="isLeaf" column="is_leaf"/>
			<result property="orderNumber" column="order_number"/>
			<result property="createTime" column="depart_create_time"/>
			<result property="status" column="dept_status"/>
		</collection>
		
		<collection property="userRoleList" ofType="sysRole">
			<id property="id" column="role_id"/>
			<result property="name" column="role_name"/>
			<result property="desc" column="role_desc"/>
			<result property="status" column="role_status"/>
			<result property="createTime" column="role_create_time"/>
		</collection>
		
		
	</resultMap>
	
	<select id="getUserWithRelation" resultMap="userWithRelationResultMap">
			
		SELECT 
			u.id AS user_id,
			u.login_id,
			u.name AS user_name,
			u.password,
			u.email,
			u.create_time AS user_create_time,
			u.status AS user_status,
			d.id AS depart_id,
			d.dept_no,
			d.dept_name,
			d.dept_name_en,
			d.dept_name_short,
			d.parent_id,
			d.is_leaf,
			d.order_number,
			d.create_time AS depart_create_time ,
			d.status AS dept_status,
			r.id AS role_id,
			r.name AS role_name,
			r.desc AS role_desc,
			r.status AS role_status,
			r.create_time AS role_create_time
			FROM sys_user u,sys_department d,sys_inter_user_department id,sys_role r,sys_inter_user_role ir 
			WHERE u.id=id.user_id AND d.id=id.dept_id AND u.id=ir.user_id AND r.id=ir.role_id AND u.status=1 
			AND r.status=1
			
			<if test=" user != null and user.id != null">
				AND u.id=#{user.id}
			</if>
			
			<if test=" user != null and user.loginId != null">
				AND u.login_id=#{user.loginId} 
			</if>
			
			
			
			
		
	</select>
	
	<insert id="insertUser" parameterType="sysUser">
		insert into sys_user(login_id,EMAIL,PASSWORD,CREATE_TIME,STATUS)
			values(#{loginId},#{email},#{password},#{createTime},#{status})
	</insert>
	
	

</mapper>

